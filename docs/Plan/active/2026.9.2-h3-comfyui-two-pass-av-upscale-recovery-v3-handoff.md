# H3 V3 恢复计划 Session Handoff

更新时间：2026-09-02

适用工作区：`E:\Projects\AIVideoGenTool`

权威计划：`docs/Plan/active/2026.9.2-h3-comfyui-two-pass-av-upscale-recovery-v3.md`

## 0. 当前结论

本 session 因用户报告 Windows 蓝屏并重启而停止。不能把本次运行描述为
`runtime-smoke-passed` 或 `product integrated`。

- WP0 的错误 UI/sidecar 隔离与安全拒绝已经落地。
- WP1 的节点 catalog、精确 revision 安装和运行时 schema 检查已经落地；蓝屏前实机扫描通过。
- WP2 的原创 AV serializer/loader、共享 artifact service 和静态测试基础已经落地；真实 serializer 写出了 payload，但还没有完成 manifest commit、loader round-trip 和 History 绑定，因此 WP2 Gate 未完成。
- WP3 的 first-pass workflow 静态检查和 `/object_info` 检查通过；真实 H3 运行没有得到完整成功证据：FP16 在 VAE decode 失败，INT8 运行在 CPU VAE decode 阶段被中断。
- WP4–WP8（真实二采、queue/history 接线、1080p/1440p UI、Ref2VA/Extend）不能继续声称已完成，也不应在前置 Gate 未通过时继续开放。

## 1. 用户安全和进程状态

用户报告上一轮任务导致 Windows 蓝屏，随后已经重启电脑。日志只能证明测试造成了明显的内存压力，不能单独证明蓝屏的直接根因；驱动、硬件、温度、电源和系统状态仍可能参与其中。

上一轮测试的风险判断是错误的：

- ComfyUI 的实际启动参数包含 `--cpu-vae`，因此视频 VAE decode 在 CPU/系统内存执行。
- first-pass workflow 在采样后使用 `VRAM_Debug` 卸载模型，随后进入 CPU VAE decode。
- 测试使用了 864×480、124 帧、20 steps，不能算作最小 smoke。
- `LocalVideoStudioH3SaveJointAV` 将两个 latent tensor 拷贝到 CPU 是正常的写文件行为；它产生的 payload 约 5.8 MB，不是主要内存来源。

本 session 后续明确停止了旧任务和服务操作：

- 不要恢复或轮询旧的 `write_stdin`/Node 等待句柄；蓝屏和重启后这些句柄不可信。
- 本 session 曾启动 ComfyUI PID `48612`，用户要求停止后已按精确 PID 终止。
- 用户明确要求“停止，不要再检测 8188”。下一 session 在用户再次明确要求前，不要自动访问、探测、启动或重启 `127.0.0.1:8188`，也不要提交任何 ComfyUI prompt。
- 不要为了“确认服务状态”循环等待一个未知进程。若将来用户明确要求运行，先取得新的、一次性的服务确认，再执行有界且无模型的测试。

## 2. 蓝屏前的可靠 runtime 证据

以下证据来自蓝屏前、ComfyUI 正常运行期间；重启后不能假设这些状态仍在当前进程中。

### 2.1 ComfyUI/core

- endpoint：`http://127.0.0.1:8188`
- ComfyUI：`0.33.0`
- Python：`3.12.11`
- PyTorch：`2.10.0+cu130`
- GPU：`NVIDIA GeForce RTX 4090`
- VRAM total：约 `25.76 GB`
- 安装核心：`D:\Comfy-Desktop\ComfyUI-Installs\ComfyUI\ComfyUI\main.py`
- 数据根：`C:\Users\Wuyouwofang\Documents\ComfyUI`
- 模型根：`C:\Users\Wuyouwofang\Documents\ComfyUI\models`
- 输出根：`C:\Users\Wuyouwofang\Documents\ComfyUI\output`

### 2.2 两个节点

蓝屏前的依赖扫描结果为 `installed=true`、`loaded=true`、`runtimeVerified=true`、`compatibilityState=supported`：

| 节点 | 位置 | revision |
|---|---|---|
| `h3-latent-upscaler` | `C:\Users\Wuyouwofang\Documents\ComfyUI\custom_nodes\h3-latent-upscaler` | `a5ed6e9586f0b14250a0018f78568e0076e4bd9d` |
| `local-video-studio-h3-av` | `C:\Users\Wuyouwofang\Documents\ComfyUI\custom_nodes\LocalVideoStudio-H3` | `0.1.0` |

已通过 live `/object_info` schema 检查的关键 class type：

```text
MiniMaxH3LatentUpscale
MiniMaxH3ConditioningUpscale
MiniMaxH3AddNoise
MiniMaxH3ShiftSigmas
LTXVSeparateAVLatent
LTXVConcatAVLatent
LocalVideoStudioH3SaveJointAV
LocalVideoStudioH3LoadJointAV
```

两个 H3 API workflow 经过当前 live `/object_info` 的静态/runtime schema 检查，返回 issues `[]`。这只是 schema evidence，不是成功生成证据。

### 2.3 模型文件

蓝屏前确认存在：

```text
models/diffusion_models/minimax_h3_fl2va_pruned_int8_convrot.safetensors
models/text_encoders/qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors
models/vae/minimax_h3_audio_vae_fp32.safetensors
models/vae/minimax_h3_video_vae_fp16.safetensors
models/vae/minimax_h3_video_vae_int8_convrot.safetensors
```

learned H3 3D upscaler 权重没有确认安装；`latent_upscale_models` 当时只有
`ltx-2.3-spatial-upscaler-x2-1.0.safetensors`。不能把这个 LTX 权重当成 H3 learned 3D upscaler。

## 3. 真实测试时间线和结果

### 3.1 FP16 first-pass

- Prompt ID：`95c10140-414a-496a-85b8-7ea0e5983d80`
- 参数：864×480、124 帧、20 steps、seed `20260902`
- 采样完成，错误发生在 `VAEDecode` 节点 `13`
- 错误：`expected m1 and m2 to have the same dtype, but got: float != struct c10::Half`
- `LocalVideoStudioH3SaveJointAV` 节点已经执行并写出 AV payload，但 MP4 未成功，因此不能算 first-pass 完成。

### 3.2 INT8 first-pass

- Prompt ID：`c476a33b-4656-47d0-86d9-1e1f0bb44c33`
- 参数：864×480、124 帧、20 steps、seed `20260902`
- 采样完成；日志显示进入 `VAE load device: cpu`
- VAE decode 长时间占用 CPU/系统内存，约 16 分钟后按边界中断
- 中断节点：`VAEDecode` 节点 `13`
- 没有完整 MP4 success，也没有 History success

已写出的外部 payload：

```text
C:\Users\Wuyouwofang\Documents\ComfyUI\output\h3-native-av\runtime-smoke-int8-mtjfvivx.safetensors
bytes: 5807592
sha256: 6339b3d91d9885620f87ee1922f49d318ea02a26434bb3c8ee0c7fd9edd784de
video_shape: [1, 24, 37, 30, 54]
video_dtype: F32
audio_shape: [1, 32, 2, 207]
audio_dtype: F32
```

另一个 FP16 试验 payload 为：

```text
C:\Users\Wuyouwofang\Documents\ComfyUI\output\h3-native-av\runtime-smoke-mtjfqix0.safetensors
bytes: 5807592
```

这两个文件是 ComfyUI output 下的未 commit payload，没有 Electron manifest，也没有写入 History；不要把它们直接视为 available artifact。除非用户明确要求，不要删除或迁移这些外部用户文件。

### 3.3 22 帧试验

Parser 修复后曾提交过一个 22 帧/4 steps/INT8 试验：

```text
Prompt ID: 24ee22fc-57b2-4ef6-aee2-cea727cf716c
```

该任务没有得到可靠的最终 history 证据；随后发生了重启/服务停止。不要根据这个 Prompt ID 推断成功，也不要恢复旧等待进程。

## 4. 已实施的关键代码

### 4.1 节点和 catalog

- 新增 app-owned package：`comfy_nodes/LocalVideoStudio-H3/`
- 节点只实现 `LocalVideoStudioH3SaveJointAV` 和 `LocalVideoStudioH3LoadJointAV`，不复制第三方 sampler/upscaler。
- Save 使用固定 safetensors keys `video`/`audio`、CPU contiguous、partial 文件和原子 rename。
- Load 只接受受管 `h3-native-av` 路径，要求 manifest、bytes/SHA、固定 keys、shape/dtype，并重建 ComfyUI `NestedTensor`；不使用 pickle。
- learned 3D 节点登记为精确 pinned revision；未许可的 learned 权重仍不能自动安装。
- bundled node 的 build copy：`scripts/copy-bundled-comfy-nodes.mjs`

### 4.2 workflow/schema/artifact

- `workflows/minimax_h3_fl2va_first_pass_av_api.json`
- `workflows/minimax_h3_fl2va_second_sample_av_api.json`
- `src/core/h3-workflow-contract.ts` / `.js`
- `electron/services/native-av-artifact.ts`
- `electron/services/h3-native-av-collector.ts`
- `electron/services/history-artifact-service.ts`
- 对应 ports、queue/history/schema 和测试文件已在当前 dirty worktree 中。

当前二采 workflow 仍有重要限制：它重新构建 H3 conditioning，而不是读取已经持久化的完整 conditioning snapshot；在 WP4 真实手动成功前不能接入产品 queue。

### 4.3 最新 runtime bug 修复

实际 ComfyUI 0.33 `/history` 返回的 serializer 形状是：

```text
outputs["20"].h3_native_av = [descriptor]
```

而不是始终嵌套为：

```text
outputs["20"].ui.h3_native_av = [descriptor]
```

已修改：

- `src/core/comfy-output.ts`
- `src/core/comfy-output.js`
- `tests/comfy-output.test.ts`
- `tests/comfy-output-service.test.ts`

解析器现在兼容两种形状，但仍只接受预期 node ID、`type=output`、`subfolder=h3-native-av`、`format=safetensors`。

Parser 修复后的 focused tests：3 个 test files、15 个 tests passed；本次恢复 session 已补跑完整 `npm.cmd run verify`。

### 4.4 本次恢复 session 的离线验证

- 已完成 `npm.cmd run verify`：155 个 test files、1192 个 tests、typecheck、生产构建和 UI 对比检查全部通过。jsdom 的 `window.scrollTo` 输出仍是既有测试噪声，退出码为 0。
- 已将 handoff 中已有的真实 serializer payload 复制到临时目录，由 `NativeAvArtifactService.commitProducedFile(...)` 流式验证并生成 manifest；未向原始 `Documents\ComfyUI\output` 写入、移动或删除文件。
- 临时 artifact 的 bytes 为 `5807592`，SHA-256 为 `6339b3d91d9885620f87ee1922f49d318ea02a26434bb3c8ee0c7fd9edd784de`；loader round-trip 成功，video 为 `[1,24,37,30,54]` / `F32`，audio 为 `[1,32,2,207]` / `F32`。
- 这证明了 artifact service 与 app-owned loader 对已有真实 payload 的离线互操作，但没有增加 MP4、History version 或当前 ComfyUI runtime evidence；WP2/WP3 仍未完成。
- `queue-history` 现在会拒绝没有 artifact 的 `available` continuation data，并将其保存为 `invalid`，避免未 commit 的 payload 被当成可继续使用的 History artifact。

## 5. 当前 Git/worktree 规则

当前工作区有大量既有 dirty 修改和新增 H3 文件；这些修改不能被视为本 session 可以清理的临时文件。

- 开始前重新执行 `git status` 和 `git diff --name-status`。
- 不得使用 `git reset --hard`、`git checkout --` 或批量删除清理工作区。
- 不要只根据 `git diff` 生成旧 patch；编辑前重新读取目标文件。
- 重点保留普通 H3、SeedVR2、History、迁移、删除保护和现有 fallback。
- 不要提交模型权重、生成媒体、机器路径、secret 或临时日志。

## 6. 下一 session 的安全入口

按下面顺序继续，不能跳过：

1. 阅读 `AGENTS.md`、本 Handoff 和 V3 计划；确认用户是否再次明确授权 runtime 操作。
2. 只做仓库内静态检查：重新读 parser diff，并运行 `npm.cmd run verify`。这一步不需要 ComfyUI，也不需要 GPU。
3. 在任何 H3 生成前，先解决测试边界：不得再使用 124 帧作为 smoke；不得在未确认内存策略时运行 CPU VAE；不得用空闲显存代替峰值证据。
4. 首选无模型的 serializer/loader round-trip。现有两个 payload 缺 manifest，必须由受管 artifact service 正确 commit 后才能测试 loader；不能手写一个“看起来像成功”的 manifest。
5. 若必须做真实 H3 runtime，须由用户明确要求并先确认新的 ComfyUI 实例；使用一次性、有界、可中止的最小测试，实时记录系统 RAM/VRAM，且不自动进入 1080p/1440p、二采或 queue。
6. 只有真实 first-pass MP4+audio、committed artifact、loader round-trip 和 History evidence 齐全，才可把 WP2/WP3 标记为完成。
7. 只有 WP4 在 ComfyUI API 外部真实成功并记录 A/V、shape、fps、显存/内存峰值后，才允许接 queue；只有 queue/History 通过后，才允许恢复 1080p/1440p UI。

## 7. 不可采信的说法

下一 session 不得使用以下表述：

- “两个节点安装了，所以 H3 二采已可用”；
- “`/object_info` 通过，所以 workflow 已完成”；
- “AV safetensors 写出来了，所以 History artifact 已 committed”；
- “INT8 没报 dtype 错，所以 first-pass 成功”；
- “22 帧任务已提交，所以 smoke passed”；
- “蓝屏一定由某个 Python 节点造成”；
- “空闲显存/单次截图就是峰值内存证据”。

正确状态描述应为：节点和 schema 的 runtime evidence 已有；serializer 有真实 payload 写入证据；完整 first-pass 和产品集成仍未通过，且下一步必须先执行安全的无模型/小边界验证。
